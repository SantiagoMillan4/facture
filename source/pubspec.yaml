name: facture
description: "Québec freelancer invoicing: French-first, dead simple, Québec-correct TPS/TVQ."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
version: 0.1.0+1

environment:
  sdk: ^3.13.4

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.3
  share_plus: ^13.3.0
  url_launcher: ^6.3.1
  in_app_review: ^2.0.12
  shared_preferences: ^2.5.5
  pdf: ^3.13.1
  in_app_purchase: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  assets:
    - assets/brand/app_icon.png
    - assets/brand/app_icon_dark.png

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

# App icons generated from assets/brand/app_icon.png.
flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/brand/app_icon.png
  # Adaptive icon background matches the logo's teal so the full-bleed
  # artwork blends on Android.
  adaptive_icon_background: "#0D6F6C"
  adaptive_icon_foreground: assets/brand/app_icon.png

# Native launch screen: plain brand background only (teal in light mode,
# near-black green in dark mode). No logo image natively: the in-app
# animated splash owns the logo moment, fading/scaling it in centered.
# A native storyboard image caused positioning quirks on iOS and would pop
# in size at the handoff to the animated splash. Android 12+ shows the
# launcher icon through the system splash API instead.
flutter_native_splash:
  color: "#0D6F6C"
  color_dark: "#091717"
  android: true
  ios: true
  web: false
  android_12:
    color: "#0D6F6C"
    color_dark: "#091717"
